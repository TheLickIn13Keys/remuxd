# Copy to .env and fill in real values (the AniList resolver plugin at /resolve
# is only mounted when all three are set). Load with:  set -a; source .env; set +a
AIO_INSTANCE=https://aiostreams.example.com
AIO_UUID=your-instance-uuid
AIO_PASS=your-instance-password
